import React from 'react';
import LoginMenu from "./LoginMenu";
import Menu from "./Menu";

const Nav = () => {
    return (
        <div>
            <LoginMenu/>
            <Menu/>
        </div>
    );
};

export default Nav;